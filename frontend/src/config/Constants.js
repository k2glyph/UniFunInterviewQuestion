
export const DOMAIN = 'http://dev.api.nepallink.info/v1/';
export const LOGIN_DOMAIN = `${DOMAIN}login`;
export const CLIENT_DOMAIN = `${DOMAIN}client`;
export const BILLING_DOMAIN = `${DOMAIN}billing`;

export const NETWORK_ERROR = 'Network Error! No network connection or cannot reach to the server';

// `${ROOT_URL}/getproductbygroup/${gid}`,
// `${ROOT_URL}/getproductgroups`,
